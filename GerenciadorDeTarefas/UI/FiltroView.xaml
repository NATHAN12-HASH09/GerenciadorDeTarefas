<Window x:Class="GerenciadorDeTarefas.UI.FiltroView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GerenciadorDeTarefas.UI"
        mc:Ignorable="d"
        Title="Filtrar as tarefas" Height="320" Width="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Satus" Margin="3" Grid.Row="0" Grid.Column="0"/>
        <ComboBox ItemsSource="{Binding Status}" SelectedItem="{Binding Status}" Margin="3" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"/>
        <TextBlock Text="Data de Criação" Margin="3" Grid.Row="2" Grid.Column="0"/>
        <Calendar SelectedDate="{Binding DataCriacao}" Margin="3" Grid.Row="3" Grid.Column="0" HorizontalAlignment="Left"/>
        <TextBlock Text="Data de Termino" Margin="3" Grid.Column="1" Grid.Row="2"/>
        <Calendar SelectedDate="{Binding DataTermino}" Margin="3" Grid.Row="3" Grid.Column="1" HorizontalAlignment="Left"/>
        <UniformGrid Columns="2" Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2">
            <CheckBox Content="Todos" IsChecked="{Binding TodasTarefas}" Margin="3" />
            <CheckBox Content="Apenas uma tarefa" IsChecked="{Binding AprenasUmaTarefa}" Margin="3" />
        </UniformGrid>
        <Button Content="Filtrar" Command="{Binding FiltrarCommand}" Grid.Column="1" Grid.Row="5" Margin="3" Padding="25,0"/>
    </Grid>
</Window>
