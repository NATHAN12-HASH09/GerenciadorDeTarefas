<Window x:Class="GerenciadorDeTarefas.UI.InputBoxView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GerenciadorDeTarefas.UI"
        mc:Ignorable="d"
        Title="{Binding TituloJanela}" Height="100" Width="500">
    <StackPanel Orientation="Vertical">
        <TextBlock Text="{Binding TituloInput}" Margin="3"/>
        <TextBox Text="{Binding Input}" Margin="3" />
        <Button Content="Salvar" Margin="3" Command="{Binding SalvarInput}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}" HorizontalAlignment="Right" Padding="25,0"/>
    </StackPanel>
</Window>
