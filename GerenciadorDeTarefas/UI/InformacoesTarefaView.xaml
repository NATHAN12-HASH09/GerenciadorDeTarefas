<Window x:Class="GerenciadorDeTarefas.UI.InformacoesTarefaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GerenciadorDeTarefas.UI"
        mc:Ignorable="d"
        Title="Informações da Tarefa" Height="450" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <GroupBox Header="Titulo da Tarefa" Margin="3" Grid.Row="0">
            <TextBlock Text="{Binding TituloTarefa}" Margin="3"/>
        </GroupBox>
        <TextBox Text="{Binding Conteudo}" Margin="3" Grid.Row="1" AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
        <Button Content="Salvar" Grid.Row="2" Margin="3" Command="{Binding SalvarTarefa}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}"/>
    </Grid>
</Window>
